t = int(input().strip())
for _ in range(t):
    word = ""
    grid = [input().strip() for _ in range(8)]
    for col in range(8):
        for row in range(8):
            if grid[row][col] != '.':
                word += grid[row][col]
    print(word)
